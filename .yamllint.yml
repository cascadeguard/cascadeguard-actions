---
extends: default

rules:
  # Action YAML files have long shell commands — 160 chars is practical
  line-length:
    max: 160
    level: warning

  # action.yml files don't use document-start (---) by convention
  document-start: disable
